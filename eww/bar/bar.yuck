(defwidget bar[]
  (box
    :class "bar"
    :orientation "horizontal"
    :halign "center"
    "oie")
)

(defwindow bar_window
  :geometry (geometry
    :x "0%"
    :y "0%"
    :width "90%"
    :anchor "top center")
  :stacking "fg"
  :reserve (struts :side "top" :distance "4%" )
  (bar)
  )

